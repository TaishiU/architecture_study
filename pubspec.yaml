name: architecture_study
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0


dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  freezed_annotation:
  hooks_riverpod:
  http:
  json_annotation:
  logger:
  very_good_analysis:

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  build_runner:
  freezed:
  json_serializable:
  mockito:
  fake_async:



  #  mockito: ^5.0.0 # 最新の安定版に合わせて適宜変更
  #  fake_async: ^1.3.1 # リトライテストのために追加


flutter:

  uses-material-design: true
